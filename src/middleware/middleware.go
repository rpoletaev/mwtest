package middleware

import (
	"github.com/rpoletaev/mwtst/src/middleware/autch"
	// "middleware/compress"
	// "middleware/convertor"
)

var (
	Autch = autch.New()
	// Compress  = compress.New()
	// Convertor = convertor.New()
)
